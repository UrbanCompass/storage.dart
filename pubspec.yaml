name: storage
description: A flutter plugin for storing things :)
version: 0.1.0
author: Compass <wes@compass.com>
homepage: http://compass.com

environment:
  sdk: ">=2.0.0-dev.68.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  path_provider: ^1.3.0
  shared_preferences: ^0.5.3+4
  flutter_secure_storage: ^3.3.1+1
  meta: ^1.1.7

dev_dependencies:
  flutter_test:
    sdk: flutter
  test_api:
  uuid:
  mockito:

flutter:
  plugin:
    androidPackage: com.compass.storage
    pluginClass: StoragePlugin
